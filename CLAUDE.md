# CLAUDE.md

## Github Repository
https://github.com/Carlo1090/InterAct_Capstone_Project.git

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview
InternTrack is a capstone OJT/internship monitoring system for Mater Dei College (Bohol, Philippines), built by a 4-person team (Group 1). It digitizes SIPP internship processes: student profiles, attendance/clock-in, weekly activity journals, evaluations, and reporting.

This is a **monorepo** with three parts:
- **repo root** — Laravel 13 REST API (PHP 8.3, MySQL/SQLite). There is no `api/` subfolder — the Laravel app lives at the top level (`app/`, `routes/`, `database/`, etc.)
- `web/` — Vue 3 SPA (Vite, Tailwind CSS v4, **TypeScript** — `.ts` files, `tsconfig.json`, and the `typescript` package are already in use under `web/src`)
- `mobile/` — React Native / Expo app (Expo SDK 56, TypeScript, expo-router). Currently just the default Expo template scaffolding — deferred until Phase 7, do not wire real auth/endpoints into it yet unless asked. It has its own `mobile/CLAUDE.md` (imports `mobile/AGENTS.md`) with an Expo-SDK-56-specific instruction to check the versioned docs at `docs.expo.dev/versions/v56.0.0/` before writing any mobile code — read it once mobile work actually starts

## Tech Stack (do not change without asking)
- Laravel 13, PHP 8.3, MySQL (SQLite for local/testing)
- Auth: Laravel Breeze + Sanctum — **cookie/session-based** for the web SPA (`EnsureFrontendRequestsAreStateful`); token-based auth for mobile is DEFERRED to Phase 7. **Login is USERNAME-based** (email is parked for future use): `users.username` is NOT NULL + unique; `users.email` is now **nullable** (unique-when-present). `LoginRequest` normalizes any of `login`/`username`/`email` into one identifier and resolves it against `username` OR `email` (via `filter_var` email detection), so username-only accounts and legacy email/demo accounts both sign in. The `User` model auto-generates a unique username for any non-muted create that omits one (`User::booted()`/`generateUniqueUsername()`); seeders set explicit usernames since `DatabaseSeeder` uses `WithoutModelEvents` (which mutes that hook). Do NOT wire email login/notifications this phase.
- Vue 3 (Composition API, `<script setup>`, TypeScript), Vue Router, Pinia
- Tailwind CSS v4 (no v3 syntax, no `tailwind.config.js` patterns that v4 removed)
- Axios for API calls with CSRF/Sanctum cookie flow (`withCredentials`, `withXSRFToken`)

## Hard Rules
1. **Do not create a duplicate migration.** Before creating any migration, check `database/migrations/` for whether a table/column change already exists. If a migration for that table exists and hasn't shipped, EDIT it instead of adding a new one.
2. **Do not add a second middleware layer or restructure app architecture** without explicitly proposing it first and waiting for approval.
3. **Stay on the finalized schema** (InternTrack_Database_Schema_v2, 20 tables). Do not rename tables/columns or add tables without asking. Key structural decisions already made:
   - `weekly_activity_logs` is split into a header table + `weekly_activity_log_entries` (mirrors the physical SIPP form) — same pattern for the separate `weekly_logs`/`weekly_log_entries` and `journal_entries`
   - Separate `programs`, `departments`, `student_profiles`, `journal_templates`, `system_settings` tables exist
   - `programs` has a composite unique constraint on `(department_id, cod e)` — departments are independent top-level units (e.g. CABM-B and CABM-H are two separate departments, not sub-units of one "CABM"); don't introduce a department→division→program hierarchy without confirming first
4. **Out of scope — do not build:** geofence clock-in, rotating QR clock-in, photo capture on clock-in, exit interview report generation.
5. **Record every change in CLAUDE.md as part of finishing the task, not as an afterthought.** Any new feature, schema change, bug fix worth remembering, or behavior change must get a corresponding bullet added (matching the existing section's style) or an existing stale statement corrected — before reporting the task complete, not after being asked. A change that isn't reflected here is invisible to the next session.


## Domain Facts
- 3 departments, 7 programs:
  - CAST: BSIT
  - CABM-B: BSBA-FM, BSBA-MM, BSBA-OM, BSA
  - CABM-H: BSTM, BSHRM
- Roles (`users.role` enum, exactly these four): `admin`, `coordinator`, `supervisor`, `student`. `supervisor` means company supervisor (see `CompanySupervisor`, linking a `supervisor`-role user to a `Company`). There is no separate "adviser/instructor" role.
- User deactivation is **soft** (`is_active = false`) — there is no hard-delete route for users, since a deactivated student/supervisor's journal entries and weekly logs must stay intact. `Admin/UserController::activate()`/`deactivate()` both already existed, but `AdminUsersPage.vue` only ever called `deactivate()` (mislabeled "Delete") with no way back — an admin deactivating a user had no UI path to reactivate them, even though the backend endpoint worked. Fixed: the button is now labeled "Deactivate", and an inactive row shows "Reactivate" instead (calling the pre-existing `activate()` endpoint) — no backend changes needed, both routes already existed. `AdminUsersPage.vue`'s role filter now offers **All Users** (default) / Student / Coordinator / Supervisor (Supervisor was missing entirely before), and `Admin/UserController::index()` unconditionally excludes `role = 'admin'` regardless of filter — this page manages day-to-day accounts, not the single admin account itself (`test_index_never_returns_admin_accounts` in `UserControllerTest.php`). Each row also gained a **View** button opening a read-only account-details modal (avatar/initials, name, username, email, role, program, department, status, `must_change_password`) — explicitly **not** a password, since passwords are hashed and unrecoverable by design; the modal says so and points to the existing "Issue Temporary Password" flow on System Settings for a locked-out user. Separately confirmed (no code change needed): a completed student's write access is **already** blocked today — `ResolvesStudentEnrollment::activeEnrollment()` (used by every student write endpoint) requires `status = 'active'`, which a `completed` enrollment row never matches, while `currentEnrollment()` (used by reads) allows `active` **or** `completed` — so "restrict a graduated student to read-only" was already the system's behavior via enrollment status, not `users.is_active`.
- Creating a `student`-role user auto-creates a matching `student_profiles` row via `UserObserver` (on the `User` model's `created` event), with a placeholder `student_id_number` (`PENDING-XXXXXXXX`) until the real registrar ID is filled in.
- SIPP compliance documents referenced by the system: OJT Annual Report, Summary Report on Student Exit Interview (report itself out of scope), Student Information Sheet.

## Roles & Configuration
- Admin creates accounts (`Admin/UserController`) and global structure (departments, programs, companies). Creating a student user does **not** enroll them — see below.
- Coordinators own `batches` for their program(s) (`Coordinator/BatchController`) and enroll students into a batch (`Coordinator/EnrollmentController`), which creates the `batch_students` row. `batch_students` is the **authoritative student↔company/supervisor linkage**, with a **DB-level `UNIQUE(batch_id, student_id)`** backstop — at most one row per (batch, student) pair, ever; a row's `status` (`active`/`completed`/`dropped`) carries its lifecycle. Student-facing **WRITE** endpoints (journal/weekly-log save + submit, info sheet, weekly activity logs) require an active (`status='active'`) row via `ResolvesStudentEnrollment::activeEnrollment()` and 422 with "You are not currently enrolled in an active OJT batch." without one; journal/weekly-log **READ** endpoints (show/index/calendar/PDF) instead use `currentEnrollment()` (latest `active` OR `completed`), so a completed student keeps full read access. **All enrollment placement flows through `App\Services\EnrollmentService::enrollOrReactivate()`** (used by `EnrollmentController::store`, info-sheet Accept, and `BatchRosterController::add`): it reuses ANY existing row for the exact (batch, student) pair regardless of status — refreshing `company_id`/`supervisor_id`/`assigned_division` and setting it back to `active` — and only creates a fresh row when none exists, which is what makes the unique index safe.
- **The OJT journal window is REAL-TIME, not estimate-bound** (`ResolvesStudentEnrollment::ojtRange()`): start = the batch's `start_date`, end = `batch_students.completed_at` once the coordinator marks the enrollment completed, or **today** (rolling) while it's still active. The info sheet's `ojt_info.ojt_start_date`/`ojt_end_date` are **informational only** — they render on the form/PDF but gate nothing. The **completion signal** is coordinator-driven from the batch roster modal (`BatchRosterController::complete`/`reopen`, `PATCH batches/{batch}/roster/{batchStudent}/complete|reopen`, confirm-first + toast like drop/reactivate): `complete` requires an active row (422 otherwise) and sets `status='completed'`; `reopen` requires a completed row and is blocked if the student is active elsewhere. `completed_at` is maintained **only** by a `BatchStudent::booted()` saving hook (stamped on entry to `completed`, cleared on exit — `EnrollmentController::update` and every other instance save get it for free; don't set it manually). Downstream: a completed student's writes 422 (`isEditableDate` also requires `status='active'`), the journal calendar freezes its upper bound at `completed_at` (days past it are `no_entry`; while active, days after today stay `future`), the student weekly-logs list ends at the completion week (it **starts** at the Monday of the student's first daily entry or earliest weekly log in the batch — empty list until some work exists), `WeeklyBundlingService` skips them (it only iterates `status='active'`; their existing logs persist), and `User::isInfoSheetGated()` treats an active **or completed** enrollment as having cleared intake so completing a legacy student doesn't re-gate them.
- Coordinators are assigned to a department via the `coordinator_departments` pivot (`User::departmentsCoordinated()` / `Department::coordinators()`), and admins manage the assignment from the Admin Departments page (`Admin/DepartmentController::assignCoordinator`/`removeCoordinator`). **As of the 2026-07-12 admin migration (`enforce_single_department_per_coordinator`), this is one department per coordinator** — `coordinator_departments.coordinator_id` is now unique, `AssignCoordinatorRequest` rejects assigning a coordinator already attached elsewhere, and `Admin/UserController::store` takes a single `department_id` (not `department_ids[]`). This superseded the earlier "a coordinator can run multiple departments" design — a department can still have multiple programs and multiple coordinators, just not the reverse. A coordinator's programs are resolved via `User::coordinatorProgramIds()` — every program in their assigned department, merged with the programs of batches they already coordinate (`batchesCoordinated()`, kept only as a backward-safety net) — not `batchesCoordinated()` alone. `users.program_id` is retained (still used by account creation/`StoreUserRequest`) but is **no longer** the coordinator scoping source.
- **Enrollment is student-driven, coordinator-gated intake — the Student Information Sheet is the gateway** (this superseded the old "enroll first, fill sheet after" model). End-to-end flow:
  1. **Coordinator creates the student login** on the Users → Interns page (`EnrollmentController::createAccount`, `CreateAccountRequest`): NAME, **USERNAME** (no email), PASSWORD, optional STUDENT ID, **PROGRAM** (scoped to their department) + **BATCH** (one they coordinate, belonging to that program). The account is created **NOT-ENROLLED**; the coordinator's intended placement is recorded as a **DRAFT `student_information_sheets` row whose `batch_id` is the intended batch** — the single home for "intended batch before Accept" (no new column). `enrollment-options` now also returns the coordinator's `batches` (with `program_id`) so the form filters the Batch dropdown by the chosen program. Account creation is still distinct from enrollment.
  2. **The student is GATED** until their sheet is `approved`: `EnsureInfoSheetApproved` middleware (alias `infosheet.approved`) wraps a gated student route subgroup in `routes/api.php` — only `student/info-sheet` (show/store), `student/info-sheet/pdf`, `student/companies`, and `student/password` stay ungated; every other student endpoint 403s. `User::isInfoSheetGated()` is the single source of truth (gated unless an approved sheet OR an active enrollment exists — the latter keeps legacy directly-enrolled students working), surfaced on `/api/user` as `student_gated` for the frontend router guard (`web/src/router/index.ts`) + `StudentLayout` nav filtering.
  3. **The student fills + submits the sheet** (`StudentInfoSheetPage.vue`, matching the client reference: Student Trainee Information + Internship Company Information; the on-screen form itself has no sketch input, though the exported PDF gained a blank sketch box — see item 5 below). `Student/StudentInfoSheetController::store()` **no longer requires an active enrollment** — it writes the scaffolded draft (or falls back to an active enrollment's batch for legacy students), re-derives the read-only Program/Coordinator from the intended batch (anti-spoof), and clears any prior rejection. **Name of Company is the one constrained field** — a dropdown from `GET student/companies` (active companies); it stores both `ojt_info.company_id` (drives Accept) and `host_company` (name). **Year Level is a constrained dropdown** (`1st Year`..`4th Year`, validated via `in:` in `StoreInfoSheetRequest`), not free text. **An approved (enrolled) sheet is no longer permanently read-only** (this supersedes the earlier "approved = fully locked" design): `store()` still blocks a fresh gate submission from replacing it, but now allows **partial post-enrollment editing** — **Program & Year and the assigned Company stay locked** (re-derived server-side from the existing sheet, ignoring whatever the client sent, since they drove the coordinator's Accept step), while personal info, company address, signatory, supervisor, schedule, and dates remain freely editable for routine profile upkeep. Saving an already-`approved` sheet **never** moves it back to `draft`/`submitted`/clears its `rejection_reason` — `submission_status` stays pinned to `approved` — so routine edits can't accidentally re-trip `EnsureInfoSheetApproved` and re-gate an already-enrolled student. For a not-yet-approved sheet, status flow is unchanged: `draft`/`submitted`/`rejected` are fully editable (`submission_status` enum has `'rejected'` + a nullable `rejection_reason`). Submit confirms first + toasts.
  4. **The coordinator reviews on Student Info Sheets** (`Coordinator/CoordinatorInfoSheetController`, `CoordinatorInfoSheetsPage.vue`) — now an **Accept/Reject queue scoped by the sheet's intended-batch program** (so NOT-yet-enrolled students finally appear; the old enrollment-only scoping couldn't surface them), with status/program filters. **Accept = enroll** (`accept`): creates the active `batch_students` row for the sheet's batch + the student's chosen company + **that company's Company Supervisor**, then marks the sheet `approved` (lifting the gate). Only a `submitted` sheet is acceptable (re-accept is a no-op 422, never double-enrolls); a chosen company with no supervisor 422s. **Reject** (`reject`, `RejectInfoSheetRequest`) sets `rejected` + a required reason shown to the student, who edits + resubmits. Both are scope-enforced (403 out of scope). Enrollment is shared via `App\Services\EnrollmentService::enrollOrReactivate()`, reused by both `EnrollmentController::store` and Accept.
  5. **Individual Info Sheet PDF** (`pdf.info-sheet` blade via the shared `BuildsInfoSheetPdf` trait): official MDC document with the logo embedded as a base64 data URI from `public/images/mdc-logo.png` (labeled-placeholder fallback if absent), the college/program header, the two labeled sections, plus a **"Sketch of Internship Company Location" box** (bordered, blank, 35mm tall) at the bottom of the page — reinstated in commit `b401733`, reversing the earlier "no sketch box" decision recorded in this file; the on-screen `StudentInfoSheetPage.vue` form itself still has no sketch input, this is PDF-only. Downloadable by the student (`GET student/info-sheet/pdf`, own) and the coordinator (`GET coordinator/info-sheets/{student}/pdf`, in-scope; 403 otherwise) — "Download PDF" buttons on both surfaces.
- The **Annual SIPP Report** (`Coordinator/AnnualSippReportController`, page `web/src/pages/coordinator/CoordinatorAnnualSippPage.vue`) is **per-program** — a secondary nav shows one tab per program in `User::coordinatorProgramIds()` scope; an out-of-scope program 403s. Candidate rows are sourced one-per-daily-journal-entry from students' `sipp:true` journal fields (`issues_concerns`/`solutions`/`recommendations`), joined to `batches` by `program_id` + `academic_year`. The coordinator curates (edit any of the 3 cells, include/exclude, delete row) — curation, signatories, and the editable "DEGREE PROGRAM" heading persist in `sipp_annual_reports.report_data` (JSON), keyed by coordinator+program+AY (`program_id` added nullable, `batch_id` made nullable). Only included rows export via dompdf (`resources/views/pdf/annual-sipp-report.blade.php`, official-document layout).
- The **HTE & Student Interns List** (`Coordinator/HteReportController`, page `web/src/pages/coordinator/CoordinatorHtePage.vue`) is a **separate** coordinator report from the Annual SIPP Report. It is a single **combined list per academic year** (not per-program tabs) covering all programs in `User::coordinatorProgramIds()` scope. Candidate rows are derived one-per-enrollment from `batch_students` (joined to `batches` by `program_id` + `academic_year`), mapped to the 5 official columns: host establishment (`company.name`), student name ("Last, First M." when a clean split exists), program (code + year level, e.g. `BSIT-4`), gender (`student_profiles.sex`), and duration (batch `start_date`–`end_date`). The coordinator curates (edit any cell, include/exclude, delete, **add a manual row** for incomplete data) and edits editable signatories; curation persists as an override layer in the new `hte_reports.report_data` (JSON), keyed by coordinator + nullable `program_id` + AY, leaving source enrollments untouched. An optional `program_id` query filter is authorized against scope (403 out-of-scope), mirroring `AnnualSippReportController`. Only included rows export via dompdf (`resources/views/pdf/hte-report.blade.php`, official-document layout).
- The four other coordinator pages are now **real + department-scoped** (via `User::coordinatorProgramIds()`, out-of-scope 403s): **Dashboard** (`CoordinatorDashboardController`) — active-intern count, journals submitted vs missing this week (Mon–today), active batches, and a "students behind" list (in-scope active interns with ≥1 `missing`/`overdue` daily entry this week); **Journal Activities** (`CoordinatorJournalActivityController`) — read-only monitoring, default = today, filterable by `from`/`to` (Y-m-d) date range + `company_id` + `program_id` + `status` (see below for the detail view), per-student submitted/missing tally over a range (use `whereDate`, not raw `whereBetween`, so same-day matches hold under SQLite's datetime storage); **Partner Companies** (`CoordinatorCompanyController`) — scoped CRUD where scope = companies used by the coordinator's department students **plus companies not yet linked to any enrollment** (no creator column exists, so unlinked ⇒ visible, which keeps freshly-created companies in view), with a supervisors panel that attaches an existing `supervisor`-role user or creates+attaches a new one inline via `company_supervisors`; **Student Info Sheets** (`CoordinatorInfoSheetController`) — read-only view of in-scope students' latest sheets (info-sheet PDF export is deferred to the Student page cleanup, marked with `TODO(student-cleanup)`). `companies.description` (nullable text) was added for the companies form.
- Coordinators can create **student or supervisor login accounts** from the Interns page (`EnrollmentController::createAccount`, `CreateAccountRequest`): role is restricted to `student`|`supervisor` only (never coordinator/admin), password hashed, `is_active` true, and a student also gets a `StudentProfile` (a student's `program_id` must be in the coordinator's scope). This is **account creation only, distinct from enrollment** — a created student still must be enrolled into a batch (company + supervisor) via `EnrollmentController::store`; the Interns page keeps the two as separate controls.
- The daily-journal length limit is **character-based**: `journal_templates.char_limit` (renamed from `word_limit`, default **1500**) validates the whole entry's combined content with `mb_strlen` in `StoreJournalEntryRequest`; per-SIPP-field cap stays at **300** chars. Coordinators set it on the Journal Templates page ("Character Limit"), and the student write page counts characters (`X / char_limit`). The journals-list `word_count` display attribute is a separate, unrelated concern.
- **Daily journal locks only once its week is bundled, not on submit** (this supersedes the earlier "one-submission lock" design — a submitted entry is no longer permanently frozen the moment it's submitted): `Student/JournalEntryController::store()` now 422s ("This week has already been compiled into your Weekly Log and can no longer be edited.") only once `isBundledWeek()` finds a `WeeklyLog` row for that date's Mon-Fri week (student+batch) — i.e. once `WeeklyBundlingService` has compiled the week at least once, via the Saturday schedule or the admin on-demand trigger. Until then, a student can freely resave any date in their editable range, submitted or not — there is still no return-for-revision concept for individual daily entries; the correction surface is the weekly narrative itself once bundled. This is a **one-way lock**: even if the resulting `WeeklyLog` is later returned by a supervisor for revision, the underlying daily entries stay locked. `isEditableDate()` is now a thin wrapper over a new `lockedReason()` (`'not_active'` | `'range'` | `'bundled'` | `null`), and `show()` exposes that `locked_reason` so `StudentWriteJournalPage.vue` can show the right banner copy instead of one generic message. The `date`-cast-column gotcha below still applies inside `store()` (it fetches the existing row via `whereDate`, not plain equality).
- **Journal templates are many-programs-per-template**: `journal_templates.program_id` was dropped in favor of the `journal_template_program` pivot with **`UNIQUE(program_id)`** (a program belongs to **at most one** template, ever — the pivot is the single source of truth). Relations: `JournalTemplate::programs()` / `Program::journalTemplates()` (both `belongsToMany`; a program's template is `journalTemplates()->first()`). Template create/update take **`program_ids[]`** (required, min 1, each in `coordinatorProgramIds()`); a program already claimed by a **different** template 422s (naming the conflict, so `UNIQUE(program_id)` never surfaces as a 500); `store/update` `sync()` the pivot. `index()` returns each in-scope program with a nullable **`assigned_template_id`** so the UI can grey out already-covered programs. A batch still resolves its template via **`batches.journal_template_id`** (unaffected by the drop); the two coordinator batch requests validate that FK against the pivot. **SIPP (Annex C) is a fixed trio** keyed `issues_concerns`/`solutions`/`recommendations` — a section may only be flagged `sipp=true` if its key is one of those three (server-guarded in `ValidatesJournalTemplate`; the frontend owns adding/removing the trio). `journal_templates.is_active` stays (soft on/off), relabeled in the UI as **"Available for use in batches"** (helper: "Turn off to retire this template without deleting its history."). The template editor (`web/src/pages/coordinator/CoordinatorJournalTemplatesPage.vue`) never shows the raw section `key` — it's always auto-generated from the section `label` (still sent in the payload). Program assignment is a **multi-select checkbox list**; a program already claimed by another template renders **disabled + greyed** ("(unavailable — claimed by another template)") via `assigned_template_id`, while programs already on the template being edited stay selectable. SIPP is authored as **one checkbox** ("Include SIPP Report (Annex C)") that adds/removes the fixed trio together inside one bordered container — there is no per-section SIPP checkbox in the UI. The modal caps its height (`max-h-[calc(100vh-4rem)]`) with a pinned header/footer and an internally-scrolling body, so a long template's form is reachable top-to-bottom (the previous centered-overlay-scrolls-itself layout made the top unreachable). The student write page (`StudentWriteJournalPage.vue`) mirrors this: the SIPP trio lives inside one bordered container revealed by a single "SIPP Report" checkbox (`sippEnabled`), respecting the 300-char SIPP field limit. On `CoordinatorBatchesPage.vue`, the batch's Journal Template dropdown filters by **membership** in `template.programs[]` against the batch's selected program (not the old single `program_id`), and templates are **refetched every time the create/edit batch modal opens** so a template created moments earlier shows up immediately.
- The two official report PDFs carry annex labels top-right: the **Annual SIPP Report is Annex "C"** (`resources/views/pdf/annual-sipp-report.blade.php`) and the **HTE & Student Interns List is Annex "D"** (`resources/views/pdf/hte-report.blade.php`); both use a clean/white table header (no gray fill) and a "(Name and Signature)" caption under each signatory block.
- **Coordinator UX rule**: crucial/destructive actions (delete a report row, deactivate a template, deactivate a batch/company, remove a supervisor/coordinator) prompt a **confirm dialog first**; successful saves show a **toast after**. Use the shared helpers in `web/src/lib/toast.ts` (`confirmAction` — a thin `window.confirm` wrapper, not a custom modal; `showToast(message, type, timeout?)`) with `web/src/components/ToastHost.vue` mounted on the page (per-page, not layout-level — a page using either helper must mount it itself). `ToastHost` renders a fixed top-right stack: white card, colored left border + matching icon (green check / red exclamation) per `type`, shadow, and a `TransitionGroup` fade/slide. This confirm-first-destructive + toast-after-success pattern is enforced consistently across coordinator, student, supervisor, and admin destructive actions (delete/remove/deactivate/drop) — not just coordinator. **Toast durations auto-scale, aren't dismissible-by-default, and are capped** (this pass hardened the previously bare-bones toast queue): `timeout` is now optional — when omitted, `showToast` derives it via `defaultTimeoutFor(message, type)` (3s base, +50ms per character past a 20-char grace window, capped at 8s; `error` toasts get a 4.5s floor even when short, since severity is its own importance signal, not just length) — none of the ~40 existing call sites pass an explicit timeout, so all of them picked up auto-scaling for free. A `MAX_VISIBLE_TOASTS = 4` cap evicts the oldest toast (clearing its timer) once exceeded, so a burst of actions can't stack the queue indefinitely. Each toast now has a close (×) button (`dismissToast(id)`) and pauses its countdown on `@mouseenter`/resumes on `@mouseleave` (`pauseToast`/`resumeToast`, both new exports) — per-toast timer state (`timerId`/`remaining`/`armedAt`) lives in a module-level `Map` in `toast.ts`, deliberately kept off the reactive `Toast` object so the template only ever sees `id`/`message`/`type`. No call site changes were needed anywhere — the improvements are entirely internal to `showToast`/`ToastHost.vue`.
- **The supervisor is tied to the company, never a manually-picked person** (this supersedes the earlier "Supervisor-by-company filtering" design, where the coordinator picked company AND supervisor as two independent dropdowns): `EnrollmentService::enrollOrReactivate()` no longer takes a `$supervisorId` parameter at all — every caller supplies only a `$companyId`, and the service derives `supervisor_id` itself from `Company::loginSupervisor()->user_id`, aborting 422 ("This company has no supervisor account yet...") if the company has none. This is centralized in the one shared service so the rule can't drift between the manual Enroll form (`EnrollmentController::store`), the batch roster's Add-Intern flow (`BatchRosterController::add`), and info-sheet Accept (`CoordinatorInfoSheetController::accept`, which now relies on this instead of resolving+aborting itself). `EnrollmentController::update()` mirrors this: changing `company_id` re-derives `supervisor_id` (and `company_supervisor_id`) from the new company rather than accepting a manually-picked one — `supervisor_id` was dropped entirely from `StoreEnrollmentRequest`/`UpdateEnrollmentRequest`/`AddRosterStudentRequest`. `Coordinator/EnrollmentController::options()` still annotates each entry in `supervisors[]` with `company_ids` (all companies that supervisor is attached to, via `CompanySupervisor`), but only so the Enroll form (`CoordinatorInternsPage.vue`) and batch Add-Intern flow (`CoordinatorBatchesPage.vue`) can show the resolved supervisor **read-only** once a company is picked — there is no Supervisor `<select>` in either form anymore. **Design notes / edges of this rule:** (1) **A company must have a login-bearing supervisor before ANY student can be enrolled there** (by design) — a company with only named-only rows (or none) 422s on every path (Enroll, Add-Intern, Accept, and a company-change on edit). This is stricter than the old model, which let any `supervisor`-role user be picked regardless of company attachment; add a login supervisor to the company first. (2) **Stale `supervisor_id` on a login swap is now self-healing** — attaching/creating a company's login supervisor (`CoordinatorCompanyController::attachSupervisor`/`createSupervisor`) calls `syncActiveEnrollmentSupervisors()`, which re-points every **active** `batch_students` row for that company at the new login (`supervisor_id`), so the common "detach old login, attach new one" flow keeps enrollments current — the student's weekly-log PDF header (`WeeklyLogController::buildHeader` → `$enrollment->supervisor?->name`) follows along. Deliberately **not** touched by the re-sync: `company_supervisor_id` (the named individual the student typed, independent of who holds the login) and **completed/dropped** rows (historical — they keep whoever supervised them). Remaining edge: detaching a login **without** re-attaching leaves active rows pointing at the former login until a new one is attached (there's no null to re-point them to, since `supervisor_id` is NOT NULL). (3) **Deactivated login supervisor no longer diverges** — `EnrollmentController::options()` intentionally does **not** filter its `supervisors[]` by `is_active` (matching `Company::loginSupervisor()`, which also doesn't), so the read-only "resolved supervisor" display and the backend agree even when a company's login is deactivated. (4) **Seeders bypass the enrollment gate** (expected) — demo seeders create `batch_students` rows directly via Eloquent (not through `EnrollmentService`), so `migrate:fresh --seed` is unaffected, but every demo company referenced by an enrollment still needs a login supervisor for the coordinator UI's later edits/re-accepts to succeed.
- **Coordinator journal entry detail + filters**: `Coordinator/CoordinatorJournalActivityController@index` (`GET coordinator/journal-activities`) additionally accepts `program_id` (must be in `coordinatorProgramIds()`, else 403) and `status` (`submitted`|`missing`, matched against `day_status` for a single day or `submitted_count`/`missing_count` > 0 for a range), and returns a `programs[]` list (the coordinator's full scope, independent of the current filter) alongside the existing `companies[]`. A new `@show` (`GET coordinator/journal-activities/{student}/{date}`) returns one student's full entry for a day — every section `label` + the text they wrote (from `batch.journalTemplate.sections`, keyed the same way the student write page keys `journal_entries.content`), plus `status`/`submitted_at`; scoped to the student's active enrollment's program (403 out of scope, 404 if no active enrollment). `CoordinatorJournalActivitiesPage.vue` adds a per-row "View" button opening a read-only modal of this, plus Program and Status filter selects beside the existing company + date-range filters.
- **Batch deactivate guard**: toggling a batch's `is_active` checkbox from on to off in `CoordinatorBatchesPage.vue`'s edit form requires `confirmAction` before the `PUT` goes out. The confirm copy is truthful, not invented — `is_active=false` on a `Batch` has exactly one downstream effect in the codebase: `SendMissingJournalEntryReminders` (the daily reminder command) skips inactive batches. Enrollment (`batch_students.status`), journal/weekly-log submission (`ResolvesStudentEnrollment::activeEnrollment()`), and reports are unaffected — they never check `batches.is_active`. Reactivating (false→true) needs no confirm. The checkbox label, an inline warning box, and the Save button turn red while a pending change would deactivate. `CoordinatorCompaniesPage.vue` mirrors this pattern for a company's `is_active` (consequence there: hidden from the enrollment company picker; existing enrollments unaffected).
- **Batch roster records are archived, not directly hard-deleted** (this supersedes the earlier "delete a dropped/completed record directly" behavior): `batch_students` gained a nullable `archived_at` timestamp (migration `2026_07_16_000002_add_archived_at_to_batch_students.php`), following the exact precedent of the existing `completed_at` column — a plain flag, deliberately **not** Laravel `SoftDeletes`/no global scope, so every existing `BatchStudent::where(...)` query anywhere in the codebase keeps returning archived rows unchanged and `status` (`active`/`completed`/`dropped`) is completely untouched by archiving. `BatchRosterController::archive()` (`PATCH batches/{batch}/roster/{batchStudent}/archive`) is allowed on a `dropped` or `completed` row (never `active`, and never twice); `restore()` (`.../restore`) clears `archived_at` and leaves `status` exactly as it was. **Permanent delete now requires archiving first** — `destroy()`'s guard changed from "not active" to `abort_if($batchStudent->archived_at === null, ...)`. `archived_at` is deliberately excluded from the model's `#[Fillable(...)]` list (matching `completed_at`), so both actions set it via direct property assignment + `save()`, never `->update(['archived_at' => ...])` (which would silently no-op under Laravel's default fillable filtering). A nightly **30-day auto-purge** (`App\Services\BatchStudentPurgeService::purgeExpiredArchives()`, `archived_at <= now()->subDays(30)`) permanently deletes stale archives — scheduled via `roster:purge-archived` (`App\Console\Commands\PurgeArchivedBatchStudents`, `routes/console.php`, `dailyAt('02:00')`) and mirrored by an admin manual demo trigger (`POST admin/roster/purge-archived/run`, `Admin/BatchStudentPurgeController`, "Archive Purge" card on `AdminSystemSettingsPage.vue`, same confirm-first + toast + result-summary shape as the existing Weekly Bundling card). Confirmed no other table has a foreign key to `batch_students.id` (journal entries and weekly logs key off `student_id`+`batch_id` directly), so the purge's hard delete cannot orphan journal/weekly-log history. `CoordinatorBatchesPage.vue`'s roster modal buckets rows by `archived_at` before `status`, so an archived row leaves its Dropped/Completed section entirely and appears only in a new "Archived" section with Restore / Delete Forever buttons. **`reactivate()`/`reopen()` now also guard on `archived_at`** (fixed after a code review caught it): both originally only checked `status`, so calling either directly on an archived row's id (bypassing the UI, which just hides the buttons) could flip a row back to `active` while `archived_at` stayed stamped — producing an active-but-archived row that `destroy()`'s `archived_at`-only guard would then let through, and that the 30-day purge would eventually hard-delete out from under an active enrollment. Both methods now `abort_if($batchStudent->archived_at !== null, 422, 'Restore this record from the archive before reactivating/reopening it.')`, forcing an explicit `restore()` first — see `test_reactivate_then_destroy_on_a_formerly_archived_row_stays_blocked` in `BatchRosterTest.php` for the regression test. `archive()`/`restore()` now return a cheap `{archived: true}`/`{restored: true}` (matching `destroy()`'s `{deleted: true}`) instead of an unused eager-loaded `fresh([...])` payload — the only caller (the roster modal) discards the response and re-fetches the whole roster via `loadRoster()` regardless.

  Two further downstream consequences of the same root cause were found and **are now also fixed**, both inside `BatchStudentPurgeService`: (1) `HteReportController::buildRows()` used to source candidate rows purely from a live `BatchStudent` query and only look up saved curation while iterating it, so a row referenced by a saved curated override that later got purged would silently vanish from the report; `buildRows()` now also renders any `report_data.rows` entry whose id has no matching live enrollment (and isn't tombstoned in `deleted_ids`) from its last-saved snapshot, exactly like a real row (`is_manual: false`, so it keeps round-tripping through future saves) — see `test_curated_row_survives_when_its_source_enrollment_is_purged` in `HteReportTest.php`. (2) `User::isInfoSheetGated()`'s legacy fallback (`BatchStudent::whereIn('status', ['active','completed'])->exists()`) could re-gate an already-graduated legacy student (no approved info sheet on file) if their sole qualifying `completed` row got archived and purged; `BatchStudentPurgeService::purgeExpiredArchives()` now skips (`protected`, not deleted) any `completed` candidate row that is a student's *sole* remaining active/completed row **and** they have no approved `student_information_sheets` row — re-evaluated fresh on every run, so a row only stays protected as long as it's actually needed (an approved sheet or another qualifying enrollment appearing later un-protects it automatically, no manual intervention). The return shape is now `array{purged, protected, cutoff}` (was `{purged, cutoff}`) — `PurgeArchivedBatchStudents`'s console output and the `AdminSystemSettingsPage.vue` "Archive Purge" card both surface the `protected` count. The admin trigger also gained an optional `now` request field (validated `nullable|date`, mirroring `WeeklyBundlingController::run()`'s `week_start`) so the 30-day cutoff can be demoed against any date instead of only the real current time — a new "As of (optional)" date input on the card. Regression coverage: `test_protects_a_completed_row_that_is_a_students_sole_gate_clearing_signal`, `test_purges_a_completed_row_once_the_student_has_an_approved_info_sheet`, `test_purges_a_completed_row_when_the_student_has_another_qualifying_enrollment`, `test_never_protects_a_dropped_row_since_it_never_counted_toward_gating`, and `test_now_override_treats_the_given_date_as_the_cutoff_basis`, all in `BatchStudentPurgeServiceTest.php`.

  `CoordinatorBatchesPage.vue`'s seven roster-row action handlers (`removeIntern`/`archiveIntern`/`restoreIntern`/`deleteForeverIntern`/`completeIntern`/`reopenIntern`/`reactivateIntern`) were consolidated behind one shared `runRosterAction({confirmMessage, request, successMessage, errorFallback, reloadBatches?, refetchCandidates?})` helper instead of each hand-rolling the same confirm → call → `loadRoster()` → toast/catch shape — this also folds in `categorizeError` (previously imported and used only by the page's initial `load()`, but not by any of these seven action handlers, an inconsistency a code review caught) so every roster action now gets the same network/validation/server error categorization instead of a fixed fallback string.
- The **Supervisor** pages (`app/Http/Controllers/Supervisor/*`, gated by a `role:supervisor` route group; pages in `web/src/pages/supervisor/*`) are **real + scoped by company** (the "my interns" scope, via the `ScopesSupervisorWork` concern — see the Company Supervisor login/named-split entry below for exactly how; a supervisor touching a student/log outside that scope 403s). The core action is reviewing a student's **weekly narrative journal** (`weekly_logs`): `SupervisorJournalController@approve` (status `approved`) and `@returnLog` (status `returned`, requires a `supervisor_comment` via `ReturnWeeklyLogRequest`); both stamp `supervisor_id` + `reviewed_at`. Only logs that are **submitted** (`submitted_at` set) and still `pending`/`returned` are reviewable — drafts and already-finalized logs 422. **Submit is real**: `POST student/weekly-logs/{weekStart}/submit` (`WeeklyLogController::submit`) sets `submitted_at = now()` + `status = 'pending'`, making `SupervisorJournalController::index()` (`whereNotNull('submitted_at')`) reachable. Because `weekly_logs.status` **defaults to `'pending'` at the DB level even for a never-submitted draft**, `submitted_at` — not `status` alone — is the signal that distinguishes "still drafting" from "submitted, awaiting review"; both `WeeklyLogController::store()`'s and `submit()`'s lock check (`submittedLockMessage()`) key off it. **Rules**: `submit` 422s "Save a draft for this week before submitting." if there's no saved narrative yet; 422s (naming whether it's awaiting review or already approved) on a double-submit while `submitted_at` is set and `status` is `pending`/`approved`; **allowed again (resubmit)** once a supervisor returns it (`status = 'returned'`) — `submit` simply re-sets `submitted_at`/`status='pending'`, `supervisor_comment` is left untouched by resubmission. `store()` (the student's "Save Narrative" autosave) mirrors the same lock: rejects edits once `submitted_at` is set and `status` is `pending`/`approved` (422), but stays editable while `status = 'returned'` — this is what makes the return-with-comment flow meaningful. The **student page** (`StudentWeeklyJournalsPage.vue`) derives a `weekState()` (`draft`/`submitted`/`approved`/`returned`) from `status` + `submitted_at`: `draft` shows Save + Submit; `submitted`/`approved` show a status banner and a read-only textarea (no action buttons); `returned` shows the supervisor's comment, re-enables the textarea, and swaps the button to "Resubmit". Same confirm/toast rule: **Return** and **Submit/Resubmit** confirm first, **Approve** toasts after. DTR/QR/geofence clock-in is a **separate pending feature — not built here**.
- **Fixed "Daily Accomplishment" journal section**: every `journal_templates.sections` entry carries exactly one structural, non-removable section — `key: 'daily_accomplishment'`, `label: 'Daily Accomplishment'`, `required: true`, `sipp: false` — the guaranteed source Weekly Bundling (below) compiles from across every department. A coordinator cannot remove, rename, or re-key it. Enforcement lives in `ValidatesJournalTemplate::prepareForValidation()` (shared by `Store`/`UpdateJournalTemplateRequest`, so both act identically): it strips any coordinator-submitted entry under that key and prepends the canonical definition **before** `rules()` validates — so omitting it, or submitting a tampered label/required/prompt under that key, never 422s; it's silently neutralized. Because this runs before validation, "at least one section must be required" is now unconditionally satisfied by the fixed section alone. A one-time migration (`2026_07_12_000003_add_daily_accomplishment_to_journal_templates`) backfilled every pre-existing template's `sections` JSON that lacked the key. `CoordinatorJournalTemplatesPage.vue` renders it as its own locked block (label/prompt shown, not editable, no remove button) above the freely-authored `customSections` list, marked "Required — used for Weekly Bundling." **Demo-data caveat**: the seeders (`CabmbUsersDemoSeeder`, `CoordinatorPagesDemoSeeder`, etc.) create `JournalTemplate`/`JournalEntry` rows directly via Eloquent, bypassing this FormRequest-level enforcement. The seeders that write **submitted** `journal_entries` (`CoordinatorPagesDemoSeeder`, `SippDemoEntriesSeeder`, `SupervisorReviewDemoSeeder`, `CabmbWeeklyDemoSeeder`) key content by `daily_accomplishment` (alongside `task_performed`, kept for the other content the seeder/template still uses) so Weekly Bundling has real text to compile against demo data; `CabmbUsersDemoSeeder` itself seeds no `JournalEntry` rows (that's `CabmbWeeklyDemoSeeder`'s job, see below), and `CoordinatorPagesDemoSeeder`'s deliberately-`missing`/`overdue` entry is left with empty content on purpose (it's meant to simulate a student who hasn't submitted).
- **Weekly Bundling** (`App\Services\WeeklyBundlingService::bundleWeek()`) auto-compiles a student's Mon-Fri `journal_entries.content['daily_accomplishment']` (only from **submitted** entries; days with none are silently skipped, no placeholder) into `"MONDAY\n<text>\n\nTUESDAY\n<text>"`-shaped `weekly_logs.narrative` — no time range, ever. It touches every student with an **active** `batch_students` row, and **never overwrites an already-submitted `WeeklyLog`** (`submitted_at !== null`); an unsubmitted draft (or no row at all) is freely recompiled/overwritten on every run — there's no way to distinguish "the student manually edited this draft" from "we auto-filled it," so re-running always refreshes drafts. Runs automatically **every Saturday at 00:00** for the week that just ended (`routes/console.php`, `RunWeeklyBundling` artisan command, `journal:run-weekly-bundling {--week-start=}`) — Laravel 13 has no `app/Console/Kernel.php`; scheduling lives in `routes/console.php` via `Schedule::command()`. An **admin-only demo trigger** (`POST admin/weekly-bundling/run`, optional `week_start`, defaults to `WeeklyBundlingService::mostRecentlyCompletedWeekStart()`) invokes the identical service on demand — the "Weekly Bundling" card on `AdminSystemSettingsPage.vue` (date picker + confirm-first button + toast + a persistent compiled/skipped-submitted summary). The student's narrative textarea and `WeeklyLogController::store()` are untouched by any of this — it's still one flow, the pre-fill is just a starting point the student can freely edit.
- **Journal document format (V2 — plain typed documents, per client reference images)**: `pdf/daily-journal-entry.blade.php` and `pdf/weekly-log.blade.php` are both **standalone minimal blades** (they no longer `@extends` `pdf/layout.blade.php`; that layout file still exists but is currently extended by **no** view — Annual SIPP / HTE were always standalone). The journals must look like plain typed documents, not application reports. **Daily** (Times New Roman 16px, black on white): line 1 = UPPERCASE student name left / program name right (borderless two-cell table for dompdf); line 2 = day label `Nth Day (MM-DD-YYYY)` (`First Day`…`Tenth Day` as words, `Day 11` onward; date `m-d-Y`); then the `daily_accomplishment` text as unlabeled justified paragraphs; then each other **filled** section as a paragraph starting with a bold inline label (`**Label:** text`). No doc title, no meta table, no Status, no empty-section placeholders — an empty section renders nothing. The day ordinal = 1-based position among the student's own entries ordered by `entry_date` (drafts included), computed server-side in `JournalEntryController` (`entryPosition`/`dayOrdinalLabel`); `show()` exposes `entry_ordinal` + `entry_ordinal_label` (plus `student_name`/`program`) so the on-screen view renders the identical label without duplicating logic. **Weekly** (Times New Roman 12px): a bold left-aligned `My OJT Journal Week N (Company)` title (N = 1-based position among that student's WeeklyLogs by `week_start`, computed identically in `WeeklyLogController::pdf()`, `SupervisorJournalController::pdf()`, and `CoordinatorWeeklyJournalController::pdf()` — the view is shared by `GET student/weekly-logs/{weekStart}/pdf`, `GET supervisor/journals/{weeklyLog}/pdf`, and `GET coordinator/weekly-journals/{weeklyLog}/pdf`, same document family), then bold uppercase day headers each followed by a plain paragraph (the `"MONDAY\n..."` block parser stays; free text degrades to a plain paragraph). **No time-range line — shift times are not stored; never fabricate one.** The weekly blade no longer prints the meta table, Status, "Weekly Narrative" label, Supervisor's Comment box, or signature block — supervisor comments remain visible in the app UI only. Client-side, the same day-block parsing lives in exactly one place: the shared `WeeklyJournalPaperView.vue` (below).
- **`JournalPaperView.vue`** (rendered by `StudentWriteJournalPage.vue`) mirrors the daily PDF **1:1** (same header line, day label via the `entryOrdinalLabel` prop from `show()`, unlabeled accomplishment paragraphs, bold inline labels on filled optional sections, Times 16). With `editable: true` each text region becomes an **auto-growing textarea styled as the paragraph it replaces** (no border until focus), mutating the SAME reactive `content` object the form fields bind to — one source of truth, no forking; while editing, an optional section stays visible as long as its key exists in `content` (the form's Include/SIPP toggles add/remove keys), so clearing text mid-edit doesn't remove the editor, but empty sections still render nowhere read-only or in the PDF. The write page has a "Document View" button (form ↔ document); the document view carries the char counter + Save Draft/Submit (same disable logic as the form). When `show()` returns `status='submitted'` the page always lands on the **read-only** document view with a plain notice — no Save/Submit/Edit buttons (one submission per date, server-enforced by `store()`); submitting prompts a confirm first (irreversible, per the confirm-first rule).
- **Shared `WeeklyJournalPaperView.vue`** (`web/src/components/journal/WeeklyJournalPaperView.vue`): read-only document-styled renderer of a weekly log — the weekly sibling of `JournalPaperView` (same white-paper card + Times New Roman treatment, at the weekly document's smaller body size) with a modest header (uppercase student name + "Week of MM-DD-YYYY to MM-DD-YYYY") above bold uppercase day headers and justified paragraphs. The `"MONDAY\ntext"` day-block parsing was **lifted out of `SupervisorJournalsPage.vue` into this component** (mirrors `pdf/weekly-log.blade.php`'s parsing; free text degrades to a plain paragraph). No time ranges and no status badges inside the paper — status/actions are page chrome around it (pages wrap it in a `bg-slate-100` preview panel). Consumed by **three** surfaces: the supervisor review modal (`SupervisorJournalsPage.vue` — approve/return, comment display, and Download PDF chrome unchanged; the daily-entries listing is collapsed into a `<details>` below the preview), the coordinator Weekly Journals page (next bullet), and the student's `StudentWeeklyJournalsPage.vue` **read-only** states (submitted/approved render the paper view; draft/returned keep the editable textarea — save/submit/resubmit mechanics untouched).
- **Coordinator Weekly Journals** (`Coordinator/CoordinatorWeeklyJournalController`; page `web/src/pages/coordinator/CoordinatorWeeklyJournalsPage.vue`, route `/coordinator/weekly-journals`, nav entry next to Daily Journal Activities): **read-only, department-scoped** observation of interns' weekly narrative journals — before this the coordinator had no weekly-journal surface at all. Scope = the log's **batch** `program_id` ∈ `coordinatorProgramIds()` (403 out of scope); never-submitted drafts are excluded (`whereNotNull('submitted_at')`, same rule as the supervisor queue). `index` paginates (20/page, Prev/Next pager) with `program_id` (403 out-of-scope) / `status` (`pending`|`approved`|`returned`) / `from`/`to` (matched against `week_start` via `whereDate`, reversed range tolerated) filters and returns the full-scope `programs[]` for the dropdown; `show` mirrors `SupervisorJournalController::show`'s payload shape **minus `reviewable`** so the shared paper view consumes one shape; `GET coordinator/weekly-journals/{weeklyLog}/pdf` reuses the **same `pdf.weekly-log` blade** and Week-N numbering as the supervisor/student PDFs (no blade fork). **No approve/return/edit actions for coordinators anywhere** — review verdicts belong to supervisors; a row's View button opens the document-preview modal (shared paper view + Download PDF) with the supervisor's comment shown read-only.
- The coordinator **Users** page (`web/src/pages/coordinator/CoordinatorInternsPage.vue`, route `/coordinator/users`; old `/coordinator/interns` redirects here) has a **secondary nav** with two lists, both department-scoped via `User::coordinatorProgramIds()`: an **Interns** tab (`EnrollmentController::interns`, `GET users/interns`) listing **every** in-scope student *regardless of enrollment*, each with an **ENROLLED / NOT ENROLLED** badge + current placement (optional `program_id` filter 403s out of scope); and a **Supervisors** tab (`EnrollmentController::supervisors`, `GET users/supervisors`). With **no `created_by` column** on `users` (adding one would be a forbidden schema change), the "supervisors the coordinator created ∪ supervisors on their students' companies" union is realized as **supervisors attached (via `company_supervisors`) to any company in the coordinator's company-scope** — companies used by in-scope enrollments plus globally-unlinked companies, mirroring `CoordinatorCompanyController::scopedCompanyIds` — deduped by user with their in-scope companies **and the distinct in-scope batches whose students they supervise** (`batch_students.supervisor_id`), so the UI can filter supervisors by company and by batch — the Interns tab has a **program filter** (feeds `?program_id=` on `users/interns`) and the Supervisors tab has **company + batch filters** (client-side over each row's `companies`/`batches`). Header actions are **tab-contextual**: Interns shows "Create Student Account" + "Enroll Student"; Supervisors shows "Create Supervisor" only. "Create Supervisor" **requires a company first** (shown first in the form, from the coordinator's `/companies` scope) + position + name/email/password, and posts to the existing company-nested `companies/{company}/supervisors/new` endpoint — a supervisor is always a Company Supervisor. Enroll stays on this page (account creation ≠ enrollment). **Batch roster management** (`Coordinator/BatchRosterController`, "View Interns" on `CoordinatorBatchesPage.vue`) is separate from the enroll flow and scoped by **batch `program_id` in `coordinatorProgramIds()`** (out-of-scope batch/student 403): **add** a student (must be **same program** as the batch, else 422; if already active in another batch it **MOVES** them — old row → `dropped`, new active row, `moved:true`, behind a wrong-batch-guard confirm; already active in this batch 422); **remove** → marks the row `dropped` (keeps history); **delete** → only allowed once `dropped` (deleting an `active` row 422s "drop first"); **reactivate** (`PATCH .../roster/{batchStudent}/reactivate`) → flips a `dropped` row back to `active` in place (same company/supervisor/division it had), blocked with a message naming the other batch if the student already has an active row elsewhere. A persistent "Reactivate" button sits next to "Delete" in the Dropped section of `CoordinatorBatchesPage.vue`'s roster modal (not a time-limited toast-undo). Same confirm/toast rule (move/remove/delete/reactivate confirm first; adds toast after). Broad **CABM-B demo data** (`CabmbUsersDemoSeeder`, idempotent) — BSA + BSBA-FM/MM/OM students (mix of enrolled/not-enrolled), companies, attached + created-on-unlinked-company supervisors, one active batch per program, plus a shared **"CABM-B Daily Journal Template"** (canonical fixed Daily Accomplishment section + SIPP trio) assigned to all four CABM-B programs via the pivot and set as every CABM-B batch's `journal_template_id` (without it, students in those batches had no sections to write with) — is owned by a **dedicated CABM-B coordinator "Maria Antonnette Balbero"** (`mdcbalbero@gmail.com`, **CABM-B only**): her world is pure CABM-B (13 students = 9 enrolled/4 not, 7 supervisors) with **no BSIT bleed-through**. The CAST/BSIT demo coordinator (`mdccore@gmail.com`) is explicitly **detached** from CABM-B (CAST only again).
- **CABM-B weekly-bundling demo data** (`CabmbWeeklyDemoSeeder`, additive, re-runnable, registered in `DatabaseSeeder` right after `CabmbUsersDemoSeeder`): a dedicated demo student `mdcbalberostudent@gmail.com` (Renz Adrian Corvera, password `password`, BSBA-FM under coordinator Balbero) enrolled in the existing `BSBA-FM 2026 Internship` batch under the existing CABM-B-scoped supervisor `cabmb.sup.fm@gmail.com` (Mr. Dennis Chua, attached to Metrobank - Tagbilaran Branch), with a full Mon-Fri week (dated two weeks before the current date) of **SUBMITTED** daily journal entries carrying real `daily_accomplishment` text — a clean target for `WeeklyBundlingService`/the admin "Run Weekly Bundling Now" trigger and for the student's own Weekly Journals page, mirroring `SupervisorReviewDemoSeeder`'s CAST-scoped pattern but scoped entirely to CABM-B. Existing CAST demo data and `CabmbUsersDemoSeeder`'s broader roster are untouched.
- **Company Supervisor login vs. named-individual split** (migrations `2026_07_15_000003_add_name_and_nullable_login_to_company_supervisors` + `2026_07_15_000004_add_company_supervisor_id_to_batch_students`): `company_supervisors` rows now come in two kinds, sharing one table. **Login-bearing** (`user_id` set, `name` null) is the company's **one shared login account** used to authenticate as `role=supervisor` and to review weekly logs — `Company::loginSupervisor()` (`hasOne(CompanySupervisor)->whereNotNull('user_id')->oldest('id')`) resolves it, and `CoordinatorCompanyController::guardSingleLogin()` 422s any attach/create of a **second** login-bearing row for the same company (naming the existing holder), while re-attaching the exact same user stays idempotent — a company can have at most one login, ever. **Named-only** (`user_id` null, `name` set, `position` now nullable too) is just a record of a real person's name/title with no login of their own — unlimited per company. `CoordinatorCompanyController::mapSupervisors()` normalizes both shapes for the frontend into `{id, user_id, name, position, display_name (name ?? user.name), is_login (user_id !== null), user}`; `CoordinatorCompaniesPage.vue` and `CoordinatorInternsPage.vue` show a Login/Named-only pill and disable the Attach/Create forms with an explanatory note once a company already has a login. **`detachSupervisor` is now keyed by `company_supervisors.id`**, not `user.id` (route param renamed `{companySupervisor}` in `routes/api.php`) — a named-only row has no user to bind to. **`batch_students` gained a nullable `company_supervisor_id` FK** (nullOnDelete) alongside the existing `supervisor_id` (still NOT NULL, still a `users.id` FK): `supervisor_id` always pins to the company's **login** account (what a supervisor authenticates as / reviews logs as), while `company_supervisor_id` records the **actual named individual** the student typed on their info sheet, which may be a different person than whoever holds the login. `CoordinatorInfoSheetController::accept()` requires a `loginSupervisor` to exist (422 "no supervisor account yet" otherwise, since `supervisor_id` is NOT NULL) and separately resolves `company_supervisor_id` via `resolveNamedSupervisor()` — a case-insensitive find-or-create against `ojt_info.supervisor_name`/`office_designation` (pre-existing info-sheet fields), matched in PHP against `name ?? user.name` to dodge SQLite/MySQL collation differences; a blank typed name resolves to `null` and the enrollment just carries the login supervisor with no named individual. `EnrollmentService::enrollOrReactivate()` gained an optional trailing `$companySupervisorId` param that, if omitted, is resolved for free by looking up the `company_supervisors` row matching `company_id` + `user_id=$supervisorId` — so the manual Enroll form and batch-roster Add-Intern flow (which only ever knew the login `user_id`) keep working unchanged; `EnrollmentController::update()`'s manual company/supervisor edit re-resolves it the same way so it never goes stale. **Supervisor "my interns" scoping changed from user- to company-based**: `ScopesSupervisorWork::supervisedCompanyIds()` finds every company where the authed user is the `company_supervisors.user_id` (a login is attached to at most one company today, but the method stays a collection in case that changes), and `supervisedEnrollments()` (`BatchStudent::whereIn('company_id', ...)`) replaces the old direct `where('supervisor_id', ...)` queries in `SupervisorDashboardController` and `SupervisorInternController` — functionally the same today (since `supervisor_id` is always the login), but decouples scoping from that column and is what makes a shared company login see the company's full roster.
- **Self-service Profile / Change Password / Activity Log, shared across all four roles** (this supersedes the earlier student-only Change Password page): a single `App\Http\Controllers\ProfileController` — deliberately **not** role-namespaced, since the behavior is identical for admin/coordinator/supervisor/student — exposes `PUT /api/profile` (name/username/email, uniqueness excludes self), `PUT /api/profile/password` (current-password check, confirmed+different+`Password::defaults()`, clears `must_change_password`), `POST /api/profile/photo` / `DELETE /api/profile/photo` (avatar upload/removal, `image|mimes:jpeg,jpg,png,webp|max:2048`, stored on the `public` disk under `avatars/`, requires `php artisan storage:link` once), and `GET /api/profile/activity` (paginated, scoped to `system_logs.user_id = auth()->id()` — the personal counterpart to the admin-wide Audit Logs page). All routes sit in one `auth:sanctum` group outside any role prefix, so a gated student reaches it same as everyone else with no special-casing. `users` gained a nullable `avatar_path` column; `User::avatarUrl()` is an accessor (`$appends`-ed) resolving it to a public URL or `null`, so every JSON user payload carries `avatar_url` for free. The old student-only `Student\PasswordController`/`student/password` route/`Student\UpdatePasswordRequest` were deleted outright rather than kept alongside the shared one. One shared Vue component (`web/src/components/profile/ProfilePageView.vue`, three tabs: Profile / Change Password / Activity Log) is reused by four thin per-role pages at `/{role}/profile` — the route itself is unchanged, but **as of this pass there is no longer a "Profile" sidebar nav item on any of the four layouts**; instead, the header avatar-or-initials badge in each layout (`web/src/layouts/{Admin,Coordinator,Supervisor,Student}Layout.vue`) is now the `RouterLink` to `/{role}/profile` (a `title="Profile"` + hover ring is the only affordance). `StudentLayout.vue`'s gated-nav filter (`isGated`) was simplified to only keep the info-sheet link — Profile no longer needs a special-case entry there since the avatar link isn't part of the filtered nav list at all, and the route stays reachable while gated exactly as before (`router/index.ts`'s gate guard already excepts `/student/profile`, unchanged). **SystemLog coverage was expanded** to make the Activity Log tab meaningful for every role, not just admin: login/logout (`Auth/AuthenticatedSessionController`), info sheet accept/reject, student enrollment, weekly journal approve/return, and daily/weekly journal submission all now call `SystemLog::record()`. **Fixed a latent bug** in this same pass: the frontend's forced-password-change redirect (`router/index.ts`) was hardcoded to `/student/change-password` regardless of role, even though `Admin/UserController::issueTemporaryPassword` can set `must_change_password` on *any* role — a non-student given a temporary password would have been bounced in a login loop with no way to actually change it. The redirect now targets `` `/${auth.user.role}/profile` ``.
- **Shared API-error categorization + retry infrastructure (pilot rollout, not yet applied everywhere)**: `web/src/lib/apiError.ts` exports `categorizeError(error, fallback?)`, which turns any thrown Axios error into a `{kind, message, fieldErrors?}` triple — `kind` is one of `network` (no `error.response` at all, e.g. offline/timeout), `validation` (422), `auth` (401/403), `not_found` (404), `server` (5xx), or `unknown`. Before this, all ~42 `axios.isAxiosError(...)` call sites across the SPA hand-rolled their own `status === 422` check with a hardcoded fallback string, and none distinguished a network drop from a 500 — both fell through to the same generic message. `LaravelValidationErrorBody` (`{message, errors?}`) was added to `web/src/types/api.ts` as the first typed shape for a Laravel validation-error body (previously accessed as untyped `any` everywhere). A new shared `web/src/components/LoadStatus.vue` (`loading`/`error`/optional `retry` props, default slot for the loaded content) replaces the copy-pasted `<p v-if="isLoading">Loading...</p><p v-else-if="errorMessage">...</p><div v-else>...</div>` three-state pattern that every page's initial-GET-on-mount function used — critically, **this is the first Retry button anywhere in the app**; previously a failed page-load left a dead-end error message with no recovery path short of a full page refresh. **Scope of this pass is deliberately a pilot, not a full rollout**: only `CoordinatorBatchesPage.vue`'s `load()` and `AdminSystemSettingsPage.vue`'s `loadSettings()` were migrated to `categorizeError` + `<LoadStatus>` (the latter also fixes a pre-existing bug where `settingsError` was set on failure but never rendered anywhere in the General Settings card — there was no error branch in that template at all). The other ~15 load-on-mount functions and ~27 submit/action catch blocks across the other 16 files still use their original ad-hoc per-page handling; migrate them to `categorizeError` (and `<LoadStatus>` where the call is a page-load rather than a form submit) incrementally as those pages are touched, rather than as a single sweeping change. **Fixed a bug this same wiring introduced**: `AdminSystemSettingsPage.vue`'s General Settings card originally reused the single `settingsError` ref for both `loadSettings()` (the `<LoadStatus>` path) and `saveSettings()` — a failed *save* set the same ref `<LoadStatus>` watches, so the whole form (all four inputs) got replaced by the error box, and clicking its Retry button called `loadSettings()`, silently discarding whatever the admin had just typed. `saveSettings()` now writes to a separate `saveError` ref (also switched to use `categorizeError` instead of its own hand-rolled unwrap), shown in the page's existing standalone error paragraph below the cards — `settingsError`/`<LoadStatus>` is now exclusively the load-failure path.
- **Global focus-visible ring + disabled-state treatment (full rollout, unlike the pilot above — this one was a uniform mechanical change, not per-page logic)**: `web/src/style.css` gained a base-layer rule giving every `input`/`select`/`textarea` a `blue-700` `:focus-visible` outline (2px, 1px offset, ~7:1 contrast against the app's white/slate-50 form backgrounds) — before this, only 3 files (`LoginPage.vue`, `JournalPaperView.vue`, `StudentWriteJournalPage.vue`) had any custom focus styling at all; everything else fell back to an unstyled browser default. Separately, every literal `disabled:opacity-50` (37 occurrences across 14 files — the entire set of buttons that disable themselves during an async action) was replaced with `disabled:grayscale disabled:cursor-not-allowed`: a disabled button now desaturates instead of fading to transparency, and shows a not-allowed cursor. The native `disabled` attribute usage itself (`:disabled="isLoading"` etc.) was already correct everywhere and untouched — this only changed the *visual* treatment tied to it. Both changes are pure CSS/class-only, no logic or template structure changed, which is why (unlike the API-error pilot above) this rolled out to 100% of occurrences in one pass rather than a subset.

## Architecture Notes
- **Routing**: `routes/api.php` defines `/api/user` (auth:sanctum) plus an `admin/*` group gated by `auth:sanctum` + `role:admin`. Role gating middleware is `App\Http\Middleware\EnsureRole`, aliased as `role` in `bootstrap/app.php`, used as `->middleware('role:admin')` or `role:admin,coordinator` for multi-role routes. It also blocks `is_active = false` accounts with a 403 before checking role.
- **Controllers**: `app/Http/Controllers/Admin/*` for admin-only endpoints, `app/Http/Controllers/Auth/*` for Breeze/Sanctum auth flows. Follow the existing Form Request + Controller pattern (one Form Request per create/update action in `app/Http/Requests/{Admin,Auth}/`) before inventing new patterns.
- **Web SPA structure**: pages live in `web/src/pages/{admin,coordinator,student,supervisor}/`, role-specific shells in `web/src/layouts/*Layout.vue` wrapping the shared `web/src/components/layout/DashboardShell.vue`. Routes are defined centrally in `web/src/router/index.ts`, gated by `meta: { requiresAuth, role }` and a global `beforeEach` guard that calls the Pinia `auth` store (`web/src/stores/auth.ts`). API calls go through the shared Axios instance at `web/src/lib/axios.ts`.
- **Dev proxy**: `web/vite.config.js` proxies `/api`, `/sanctum`, `/login`, `/logout`, `/register`, `/forgot-password`, `/reset-password` to the Laravel backend (`VITE_BACKEND_URL`, default `http://localhost:8000`) so the Vite dev server at `:5173` can make same-origin requests.
- **CORS/Sanctum config**: `FRONTEND_URL` and `SANCTUM_STATEFUL_DOMAINS` in `.env` must match wherever `web/` is actually running — check `.env.example` when debugging auth/CORS issues.
- **Deployment (production hosting)**: `web/` (the Vue 3 SPA) deploys to **Vercel** (static build — a good fit, since it's just a Vite-built static site). The Laravel API deploys **separately, on a different host** (not yet decided) — Vercel does not natively support PHP/Laravel: no persistent process for the scheduled console commands in `routes/console.php` (weekly bundling every Saturday, the nightly 2am archive purge, missing-journal reminders), an ephemeral function filesystem, and no built-in persistent database — so only the SPA half of this monorepo goes there. Because the SPA and API will live on different domains once deployed (unlike local dev, where `web/vite.config.js`'s proxy makes them same-origin), `FRONTEND_URL`/`SANCTUM_STATEFUL_DOMAINS` must be set to the real Vercel production domain, and Sanctum's session cookie needs `SameSite=None; Secure` for the cross-site credentialed requests to keep working. The API host is expected to have an **ephemeral filesystem** (container-based, not a persistent VPS disk) — this is why avatar storage should move off the local `public` disk to an S3-compatible provider (Cloudflare R2 recommended: free tier that doesn't expire, no egress fees, drop-in compatible with the `s3` disk already defined in `config/filesystems.php`) before that API deploy happens; not yet implemented as of this note.
- **Gotcha: `date`-cast columns and plain equality under SQLite.** MySQL's `DATE` column type truncates any time component on write; SQLite's does not (SQLite has no real column-type enforcement), so a `date`-cast Eloquent attribute written as `'2026-07-06'` can come back out of SQLite storage as `'2026-07-06 00:00:00'`. A plain `where('week_start', $dateString)` — or `updateOrCreate()`'s match array, which is the same thing internally — silently fails to find the row it just created, and a second write inserts a duplicate instead of updating. Always use `whereDate('week_start', $dateString)` for lookups against a `date`-cast column (already the pattern in `WeeklyLogController::show()`/`index()`); this bit both `WeeklyBundlingService::bundleWeek()` and `WeeklyLogController::store()` in the same session before being fixed the same way — check any new code that reads back a `date`-cast column it just wrote.

- **Security-audit hardening pass**: three fixes from a full-codebase security review. (1) Route params named `date`/`weekStart` (`journal-entries/{date}`, `journal-entries/{date}/pdf`, `weekly-logs/{weekStart}`, `weekly-logs/{weekStart}/pdf`, `weekly-logs/{weekStart}/submit`, `journal-activities/{student}/{date}`) are now constrained via `Route::pattern('date', '\d{4}-\d{2}-\d{2}')`/`Route::pattern('weekStart', ...)` at the top of `routes/api.php` — previously a malformed segment reached the controller's unguarded `Carbon::parse()` and threw an uncaught exception (a stack-trace-leaking 500 if `APP_DEBUG` were ever left on), instead of 404ing at the router. (2) `AppServiceProvider::boot()` now throws a `RuntimeException` if `app()->environment('production')` and `config('app.debug')` are both true, since `.env.example` ships `APP_DEBUG=true` as a local-dev default with nothing else guarding against that value being carried into a real production `.env`. (3) `ProfileController::uploadPhoto()` re-encodes the stored avatar via GD (new private `reencodeImage()`, format-matched: `imagepng`/`imagewebp`/`imagejpeg`) immediately after `store()`, so a polyglot file (valid image container smuggling a non-image payload) can't survive on disk unmodified — defense in depth beyond the existing `image`+`mimes` validation in `UpdateProfilePhotoRequest`. No schema/route-shape changes; full test suite passes except the pre-existing weekend-flaky `SendMissingJournalEntryRemindersTest`.

- **Avatar upload pipeline overhaul (client-side crop + server-side standardization)**: `ProfileController::uploadPhoto()` no longer stores the uploaded file as-is with a light re-encode (the prior session's `reencodeImage()`, now removed as superseded); it now runs every avatar through a new `App\Services\AvatarProcessingService` (`sniffType()` + `toAvatarWebp()`) that (1) independently re-verifies the upload is a genuine JPEG/PNG/WebP by reading its actual image header via `getimagesizefromstring()` — a real magic-byte check, not the extension/declared-MIME-type trust that `UpdateProfilePhotoRequest`'s pre-existing `mimes:jpeg,jpg,png,webp|max:2048` rule alone provides (wired in via a new `withValidator()` hook on that FormRequest, so a spoofed-extension/MIME upload still 422s on `photo` even when the base `mimes` rule doesn't catch it — see `test_profile_photo_upload_rejects_garbage_bytes_disguised_as_an_image` in `ProfileControllerTest.php`), then (2) center-crops to a square, resizes to exactly 250×250, and re-encodes as WebP (quality 82) via GD — re-encoding through a fresh `imagecreatetruecolor` canvas never carries EXIF forward, so metadata-stripping (including GPS) falls out of the resize step for free, no separate call needed. The stored filename is always a fresh `avatars/{Str::random(40)}.webp` — the original filename/extension is fully discarded, never reused. Upload order is write-new-then-delete-old (not the previous delete-then-write), so a mid-request failure can't leave a user with no avatar. Storage stays on the existing `public` disk/local convention (no cloud/S3 wiring added — `config/filesystems.php` already defines an `s3` disk for later if ever needed, but that's a disk-config + `league/flysystem-aws-s3-v3` change, intentionally out of scope here). On the frontend, `ProfilePageView.vue`'s `onPhotoSelected` no longer auto-uploads on file selection — it first client-side validates type/size (specific "Invalid file type"/"File too large" messages before ever hitting the network), then opens a new `web/src/components/profile/AvatarCropperModal.vue` (plain canvas + pointer events, no new npm dependency) giving a real-time preview with pan/zoom and a circular crop guide; only on confirm does it POST the cropped square blob to the unchanged `/api/profile/photo` endpoint, now also showing an actual animated SVG spinner (previously just "Uploading..." text) during the request.

- **Notification bell (header, all four roles) + Company Representatives + Head-of-Company contact fields**: three additive changes, no other schema/route restructuring. (1) A `notifications` table + `Notification` model + `User::notifications()` already existed as part of the finalized schema and were already being written to by `SendMissingJournalEntryReminders`, but nothing read them back — a new un-namespaced `NotificationController` (mirrors `ProfileController`'s pattern) now exposes `GET /api/notifications` (paginated list + `unread_count`), `POST /api/notifications/read-all`, and `POST /api/notifications/{notification}/read` (403 unless owned), and a new shared `web/src/components/notifications/NotificationBell.vue` — mounted in all four `*Layout.vue` headers next to the existing avatar link — polls the unread count every 60s, shows a small red dot when `unread_count > 0`, and on opening its dropdown fetches the list and marks everything read (dot clears). The sidebar's pre-existing hardcoded `badge: '4'` on Coordinator's "Daily Journal Activities" nav item is unrelated dummy placeholder data and was intentionally left untouched. (2) **Company Representatives** are purely informational contacts (name + position, no login) — reusing the existing `company_supervisors` table's named-only row support (nullable `user_id`) rather than a new table, since this was confirmed as informational-only and distinct from OJT supervision. `CoordinatorCompanyController::addRepresentative()` (`POST coordinator/companies/{company}/representatives`, new `AddCompanyRepresentativeRequest`, both `name`/`position` required) creates a `user_id`-null row; removing one reuses the existing `detachSupervisor()` unchanged (it already deletes by `company_supervisors.id` regardless of login/named-only). `CoordinatorCompaniesPage.vue`'s Manage Company modal now shows two clearly separated sections instead of one: **"Company Representatives"** (rows with `!is_login`, add form + Remove) and **"OJT Supervisor Login"** (renamed from "Company Supervisors" — rows with `is_login`, the pre-existing attach-existing/create-new-login flow, unchanged in behavior since that one still drives weekly-log review). The companies table's single "Supervisors" count column was split into "Representatives" (named-only count) and "OJT Supervisor" (Yes/None badge, since at most one). (3) **Head of Company** gained dedicated contact fields, separate from the company's general `contact_number`: new nullable `companies.head_contact_number`/`head_email` columns (migration `2026_07_18_000001_add_head_contact_fields_to_companies_table`, additive since the original `create_companies_table` migration had already shipped), added to `Company`'s fillable list and both `Store`/`UpdateCompanyRequest` (`head_email` validated as `email`). The pre-existing `department_head` field is untouched. Frontend: both new inputs added under a "Head of Company" subheading in the same modal, grouped with the existing `head_name`/`department_head` fields.

## Roadmap Context
Seven-phase roadmap. Phases 1–2 (project scaffolding, auth, base schema, admin identity/role management) are complete. Coordinator-facing pages/module work is in progress (see `web/src/pages/coordinator/*`). Mobile (Expo) integration is Phase 7 — do not wire mobile auth or endpoints for it yet unless asked.

## Conventions
- Laravel: RESTful API controllers, Form Requests for validation, Policies for authorization once introduced. Follow existing patterns in the repo before inventing new ones.
- Vue: pages in `src/pages/<role>/`, shared components in `src/components`, Pinia stores in `src/stores`, types in `src/types`. Match the existing folder structure — inspect before creating.
- Naming: snake_case for DB columns, camelCase in TS/JS, StudlyCase for PHP classes.
- Seeders must remain re-runnable (`php artisan migrate:fresh --seed` should always work).

## Verification — Evidence Before Claims
Never say something is "done", "fixed", or "working" without running a check and showing the output:
- Backend changes: `php artisan test` (or the specific test), and `php artisan migrate:fresh --seed` when migrations changed
- Frontend changes: `npm run build` (in `web/`) must succeed; run `npm run lint` if configured
- After fixing a bug: reproduce it first if possible, then show the passing result

## Common Commands
```bash
# API (run in repo root)
php artisan serve
php artisan migrate:fresh --seed
php artisan test
php artisan test --filter=TestName   # single test
php artisan test tests/Feature/Coordinator   # a whole role folder (tests/Feature splits into Admin/Auth/Console/Coordinator/Services/Student/Supervisor)
composer install
./vendor/bin/pint   # Laravel Pint code style fixer (dev dependency, no composer script wired up)

# Or run backend + queue + logs + Vite together:
composer run dev

# Web SPA (run inside web/)
npm install
npm run dev
npm run build   # web/package.json has no lint or test script configured — there is no frontend test runner yet

# Mobile (run inside mobile/)
npm install
npx expo start
```

Seeded demo accounts (from `php artisan db:seed`, password `password`) for manual login verification — **log in by USERNAME** (email is parked; email still works for accounts that have one): `mdcadmin` (admin), `mdccore` (CAST/BSIT coordinator), `mdcbalbero` (CABM-B coordinator "Balbero"), `mdcstudent` (enrolled student), `mdcsupervisor` (supervisor). `system` is a non-login automation account. **Intake-flow demo** (`CabmbIntakeDemoSeeder`, CABM-B under Balbero, both NOT-enrolled, re-armed each seed): `mdcintake` has a **draft** sheet (log in gated → fill → choose company → submit) and `mdcintake2` has a **submitted** sheet for Bohol Quality Corporation naming `supervisor_name: "Mr. Ramon Delgado"` (deliberately distinct from that company's login supervisor "Ms. Carmela Uy"), so it sits in Balbero's Student Info Sheets "Submitted" queue ready to **Accept** — a live demonstration of the Company Supervisor login-vs-named-individual split (below): Accept resolves the login (`supervisor_id`) and this named individual (`company_supervisor_id`) as two distinct rows.

## Workflow Preferences (project owner)
- Propose a plan for any multi-file change before implementing; wait for approval.
- Prefer direct numbered steps with exact terminal commands over conceptual explanations.
- When a task touches the database, always cross-check the schema first.
- If instructions in this file conflict with what you find in the repo, stop and ask instead of guessing.
